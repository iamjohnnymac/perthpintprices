# GSC #229 evidence

`terminal-proof.txt` is the non-secret output of the repeatable classification run. `terminal-proof.png` is a faithful rendered capture of that output, inspected before this handoff. It records 99 crawled-not-indexed rows, 18 discovered-not-indexed rows, 91 joined pub rows, their data-field cohorts, the one expected permanent-closure sitemap exclusion, zero unexpected HTTP/canonical/sitemap exceptions, strict validation-test success, and the zero-result secret-value pattern scan.

The command used Infisical's scoped process injection and made public production GET requests plus one read-only `pubs` select. It does not contain export locations, sessions, credential values, or an indexing request.
