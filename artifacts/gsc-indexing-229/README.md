# GSC #229 evidence

`terminal-proof.txt` is the non-secret output of the exact required classification command, including its report date, export date and output destination. `terminal-proof.png` is a faithful rendered capture inspected before handoff. It records 99 crawled-not-indexed rows, 18 discovered-not-indexed rows, 91 joined pub rows, their data-field cohorts, the one expected permanent-closure sitemap exclusion, zero unexpected HTTP/canonical/sitemap exceptions, five strict validation tests, the complete CI-equivalent checks, and the zero-result secret-value pattern scan.

The command used Infisical's scoped process injection and made public production GET requests plus one read-only `pubs` select. Its `/tmp/perth-pint-gsc-sanitized/` inputs contain only the two reviewed URL/date tables. It does not contain browser-export locations, sessions, credential values, or an indexing request.
