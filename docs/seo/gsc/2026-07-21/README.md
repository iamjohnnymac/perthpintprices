# URL-level GSC cohort classification — 2026-07-21

Generated by `scripts/build-gsc-indexing-baseline.mjs` from authenticated, read-only browser exports. The committed CSV deliberately retains only URL and report-derived fields plus current public/Infisical-joined evidence; no browser paths, ZIPs, sessions, or credentials are retained.

## Reconciliation

- GSC snapshot: **2026-07-10**; export: **2026-07-21**.
- Crawled — currently not indexed: **99/99** classified.
- Discovered — currently not indexed: **18/18** classified. Its `1970-01-01` export values are normalized to **N/A**, because GSC uses that value for unavailable crawl dates.
- Current inventory context: **849 routable rows; 833 legitimate/indexable pubs; 16 independently confirmed permanent closures**. The segmented sitemap contains **1015 URLs** (content 32; suburbs 150; pubs 833).
- Suburb cohort: **1** URL(s). Aggregate suburb context: 150 routable suburb groups; 150 with a legitimate/indexable pub; 833 legitimate/indexable pubs; 199/833 with a current stored price.
- Stored URL Inspection observations belong only in their dated source evidence; this refresh does not carry them forward unless supplied as input.

## Route mix

- article: 2
- article-index: 1
- content: 4
- guide: 3
- happy-hour-day: 7
- insight: 4
- pub: 91
- suburb: 1
- transport-hub: 4

## Current public checks

Every legitimate cohort URL currently returned 200, self-canonical and sitemap-listed. One independently confirmed permanent closure is correctly excluded from the pub sitemap. GSC membership is therefore historical/recent-publication evidence, not a present price-quality indexability defect.

## Initial HTML evidence

`initial_html_useful` is deterministic and does not use a word count: the server response must include an H1, at least one same-origin path link, and either a non-zero schema.org ItemList count (current answer/data) or methodology text. `initial_html_evidence` records all four observed values for every URL. This closes the evidence gap for each happy-hour day and transport hub.

## Visible inbound-link graph

Inbound counts, shortest homepage depth and concrete source paths come from `<a href>` elements rendered in Playwright Chromium at a fixed 1280×900 viewport across every current sitemap URL. Sitemap membership seeds the crawl inventory but is never counted as an inbound link. Links with a hidden, inert, `aria-hidden`, CSS-hidden, fully transparent or empty-layout ancestor are excluded, and URLs with no rendered inbound source record an explicit zero.

## Actionable follow-through

- **#235 (content depth):** use the CSV’s pub tier, price/happy-hour completeness and description fields to prioritize legitimate Tier C and missing-description pubs. These are enrichment cohorts only; none justify noindex or sitemap removal.
- **#237 (internal linking/schema):** use content and route-type rows to test normal visible inbound paths and HTML usefulness. Prioritize the 18 N/A-crawl new content routes and any row marked `limited` HTML.
- **#236 reconciliation:** Henley Brook’s one historical canonical mismatch is now 200/self-canonical; `/guides` and `/insights` are intentional redirects; all 15 historical 404 examples and current handling are inventoried in `docs/seo/index-cleanup-2026-07-21.md`.

## Reproduce

1. Export the two GSC tables through an authenticated read-only browser session outside the repo.
2. Run the script with **Infisical injection** (never paste or save keys):

`infisical run --projectId 3ae28e74-fc1f-4f02-beee-94100ba1e32f --env=prod -- node scripts/build-gsc-indexing-baseline.mjs --report-date YYYY-MM-DD --export-date YYYY-MM-DD --output-dir docs/seo/gsc/YYYY-MM-DD --crawled /secure/cni/Table.csv --discovered /secure/dni/Table.csv`

3. Inspect the generated CSV and terminal evidence; run `git diff --check` and secret scanning before committing.

The script makes only GET requests to production and a read-only Supabase select. It never requests indexing, changes GSC, or writes production data.
