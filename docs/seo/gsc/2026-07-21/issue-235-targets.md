# Issue #235 target selection

This is a deliberately small content pass. The #229 URL-level baseline records all three selected routes as **Crawled — currently not indexed**, with `initial_html_useful=limited`, no methodology text and one visible inbound source (`/discover`).

| URL | GSC reason | Why this pass |
| --- | --- | --- |
| `/guides/cozy-corners` | Crawled — currently not indexed (4 Jul 2026) | Adds visible, server-rendered tagged-pub facts, selection logic, freshness and useful onward routes. |
| `/guides/sunset-sippers` | Crawled — currently not indexed (25 Apr 2026) | Adds the same evidence but explains the sunset-specific selection limit. |
| `/guides/punt-and-pints` | Crawled — currently not indexed (17 Mar 2026) | Adds the TAB-on-site method and separates it from live race information. |

`/happy-hour` is not in the #229 rejected cohort, so it is intentionally out of scope. No venue page is changed, no venue is noindexed, and missing or stale prices do not affect whether a pub remains indexable.
